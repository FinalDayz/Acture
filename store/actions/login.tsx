import User from '../../models/user';

export function fetchLogin() {
    return async dispatch => {
        try {

            dispatch({});
        } catch (err) {
            throw err;
        }
    }
}