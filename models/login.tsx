class login {
    // ignore the errors
    constructor(
        loginsuccessful,
        jwtToken
    ) {
      this.loginsuccessful = loginsuccessful;
      this.jwtToken = jwtToken;
    }
  }
  
  export default login;